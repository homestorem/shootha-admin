node_modules

# Local secrets — never commit (use .env.example as template)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Expo local state (expo-doctor / EAS)
.expo/
